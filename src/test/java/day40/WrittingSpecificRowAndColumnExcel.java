package day40;

public class WrittingSpecificRowAndColumnExcel {
    public static void main(String[] args) {

        /*FileOutputStream file = new FileOutputStream("Excel file location paste here");//System.getproperty("user dir")+"path" for dynamic
        XSSFWorkbook workbook = new XSSFWorkbook();
        XSSFSheet sheet = workbook.createSheet("data");

        XSSFRow row = sheet.createRow(3);
        XSSFCell cell = row.createCell(4);

        cell.setCellValue("Welcome")

        workbook.write(file); //attach workbook to the file
        workbook.close();
        file.close();

         */
    }
}
